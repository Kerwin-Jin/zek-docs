on: push

jobs:
  pnpm-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: pwd
    - run: npm install pnpm -g
    - run: pnpm --version
    - run: pnpm install
    - run: pnpm docs:build